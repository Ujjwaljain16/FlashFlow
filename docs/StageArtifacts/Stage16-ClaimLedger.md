# Stage 16 — Claim Ledger

Every major claim this project makes, traced to its exact evidence. Status values: **SUPPORTED** (the
evidence directly backs the claim as stated), **LIMITED** (true within a stated, narrower scope than the
claim might suggest at a glance), **RETIRED** (a claim an earlier stage made that later evidence
falsified or replaced), **UNRESOLVED** (an open question, not yet answered either way).

| ID | Claim | Evidence | Scope | Status |
|---|---|---|---|---|
| C01 | TCP connection reuse reduces per-request overhead; TIME_WAIT exhaustion is a real, measurable failure mode under sequential benchmarking | Stage 1, 120-run connection-lifecycle benchmark, `experiments/001-tcp-connection-lifecycle/` | Local benchmark, one OS/network stack | SUPPORTED |
| C02 | A shared reverse-proxy code path correctly forwards requests, strips hop-by-hop headers, and forwards Content-Length instead of always chunking | `internal/proxy/proxy.go`, `internal/httpx/headers.go`; `TestProxy_ForwardsContentLength_NotAlwaysChunked` | Real HTTP engine only | SUPPORTED |
| C03 | Round-robin, weighted-round-robin, least-connections, EWMA, and P2C are each correctly implemented per their textbook definitions | `internal/proxy/{round_robin,weighted_round_robin,least_connections,ewma,p2c}.go` + per-policy unit tests | Both engines | SUPPORTED |
| C04 | The Adaptive router combines exactly four scored signals (Load, Latency, Cache, Cost); Health is a hard pre-filter, not a fifth score | `internal/proxy/adaptive.go`, 13 test cases | Both engines | SUPPORTED (historically mis-described as "six-signal" in early docs; corrected) |
| C05 | TTL caching + request coalescing (singleflight) prevents redundant concurrent fetches for the same key without serving stale data past its TTL | `internal/cache/{cache,coalesce}.go` + tests | Both engines | SUPPORTED |
| C06 | Stale-While-Revalidate serves a stale response immediately while a background refresh runs, within a configured stale window | `internal/cache/swr.go` + tests, wired into the real `EdgeServer` | Real engine | SUPPORTED |
| C07 | LRU cache eviction is implemented | — | — | **RETIRED (never a real claim)** — never built; deliberately deferred, no experiment ever needed bounded cache memory |
| C08 | The 4-state health machine (HEALTHY/DEGRADED/UNHEALTHY/RECOVERING) transitions correctly and deterministically under the virtual clock | `internal/health/*` + tests | Both engines | SUPPORTED |
| C09 | Failure injection (declarative YAML chaos schedules) correctly removes and restores target availability at specified times | `internal/chaos/{virtual,real}.go`; used in Stages 12-15 | Both engines | SUPPORTED |
| C10 | Health-registry exclusion of a crashed target is immediate | — | Subject to probe-interval detection lag (default 100ms) | LIMITED — confirmed directly in `014h`: 5.2-10.4% of during-failure decisions still targeted the crashed target, matching the expected one-probe-cycle lag, not a routing bug |
| C11 | The virtual-time engine produces byte-identical, deterministic results given the same seed | `internal/vtime/*`; identity/divergence/isolation tests re-run with `-count=5 -shuffle=on` | Same machine, same Go toolchain version — NOT claimed cross-hardware | SUPPORTED, scope-limited |
| C12 | The statistics toolkit (percentile, Mann-Whitney U, Cliff's Delta, bootstrap CI) produces correct results, not just non-panicking ones | `internal/statistics/*`; each checked against hand-computed reference values | General numeric correctness | SUPPORTED |
| C13 | Counterfactual replay produces a true exogenous/endogenous isolation: two policies replayed against the identical arrival trace diverge ONLY after the point their decisions differ | `internal/replay/*`; full-trace `reflect.DeepEqual` identity/divergence tests | Virtual engine | SUPPORTED |
| C14 | Random Search is a sufficient tuner for this project's own (small) parameter space; LHS and Bayesian Optimization do not meaningfully improve on it | `cmd/experiment-010a`'s 3-way comparison | This project's specific 6-parameter Adaptive weight space | SUPPORTED, narrow scope — not a general claim about search-method superiority |
| C15 | A minimal finite-capacity (FIFO, per-target slot count) model reveals a sharp EWMA-vs-Adaptive reversal at Capacity=1 in one severe-heterogeneity scenario | Stage 12, `012a`-`012e`; Cliff's Delta=1.000 across 12 seeds | One 3-target topology, one workload family | SUPPORTED, narrow scope |
| C16 | The Capacity=1 reversal generalizes as "ρ≈0.89-0.97 predicts the transition" | `013b`, `013c` — two independent sweep dimensions converge on the same zone | One severe-heterogeneity 3-target topology family | LIMITED — real and reproducible within that family; explicitly NOT validated as a cross-family general law even by Stage 13 itself |
| C17 | ρ (rho, offered load / capacity) remains a sufficient severity predictor as target count grows | `014a`, `014c` — achieved rho DECREASES as N grows (0.915→0.833→0.716) even as EWMA's severity WORSENS | N∈{3,5,8}, graduated topology | **RETIRED** — rho is necessary but became insufficient; `015e`'s cross-topology test later quantified this precisely (rho's rank-distance from severity = 4, badly misordered) |
| C18 | Concentration (top1/entropy) alone predicts collapse | `015b` F1: Adaptive at load far under capacity reaches top1_share=1.000 with ZERO congestion and ZERO committed backlog | Virtual engine, one scenario | **RETIRED** — concentration is necessary but not sufficient, confirmed by direct intervention |
| C19 | "Load-blind vs. load-aware" routing is the deepest regime boundary separating stable from unstable policies | `013i` (Stage 13's own multi-policy regime map) | N=3, one topology | **RETIRED** — `014f` found EWMA (load-aware by signal) loses to round-robin (load-blind) at N=8; confirmed 10/10 independent seeds in `014i` (Cliff's Delta=1.000) |
| C20 | Committed backlog (dispatches to a target between congestion onset and material diversion) is the strongest available retrospective severity measure for ACUTE collapse | `015e`: perfect rank agreement (distance=0) across a 4-point cross-topology test where peak rho is badly misordered (distance=4) | Virtual engine; topology-SIZE generalization confirmed; workload-SHAPE generalization imperfect (distance=2, not 0). Retrospective, not online: `FindPeakEpisodeCongestionOnset` anchors its window to whichever episode contains the run's peak depth, which requires having already seen that episode's own future -- a post-hoc explanatory statistic, not a live/circuit-breaker-usable predictor. An earlier synthesis called this "the strongest available predictor" without that caveat; corrected via independent audit | SUPPORTED, explicitly bounded — see C22 |
| C21 | There are (at least) two distinct collapse shapes: acute over-commitment and chronic under-provisioning | `015a`/`016-flagship`: round-robin shows LOW committed backlog (4) yet high fraction-of-time-over-capacity (0.711) and never drains; Adaptive shows the opposite pairing | Canonical scenario, replicated across 3 independent seeds in Stage 16 | SUPPORTED |
| C22 | Committed backlog is a universal severity predictor across any workload shape | `015e` Part 2: rank distance 2 (not 0) across Constant/Burst/FlashCrowd | Cross-workload | **LIMITED, explicitly not claimed as universal** — real predictive value, imperfect generalization across workload shape |
| C23 | Adaptive's resistance to committed-backlog collapse is attributable specifically to its Load (current in-flight pressure) signal, not its Latency smoothing | `015c`: ablation — removing Load more than doubles committed backlog (86→206, worse than EWMA's own 97); removing Latency has a smaller effect; removing Cache is nearly a no-op | Canonical scenario, virtual engine | SUPPORTED — a controlled intervention, not a correlation |
| C24 | Adaptive is "safe" from collapse in general | `015a`/`016-flagship`: Adaptive's own P99 (4072-4853ms) was worst-of-six in 2 of 3 seeds and a statistical near-tie with EWMA (4732 vs 4718ms, within 0.3%) in the third; in no seed did it fall into the safer half of the six policies. An earlier synthesis overstated this as "worst in every seed" -- seed 16000 actually has EWMA (4399.88ms) worse than Adaptive (4072.11ms); corrected via independent audit, see `experiments/016-final-synthesis/results/016-flagship-results.json` | Canonical scenario | **RETIRED** — mean latency alone would have hidden this; P99 does not |
| C25 | P2C and EWMA are mechanistically distinct (sampling-based bounded concentration vs. smoothed-history lock-in), not just different by chance in one scenario | `015b` F6: 8 independent seeds with genuine arrival-stream jitter — EWMA shows committed_backlog>50 in 8/8 seeds; P2C-load in 0/8 | Canonical scenario | SUPPORTED — Cliff's-delta-level, seed-independent separation |
| C26 | A validated real concurrency ceiling (`MaxConnsPerHost`) reproduces the virtual model's policy-regime reversal | `014e`: below/near/above ceiling levels, Adaptive beats EWMA cleanly and monotonically on P99 once the ceiling matches the virtual model's own Capacity=1 assumption exactly | Real engine, one 3-edge topology, one ceiling value | SUPPORTED, narrow scope — an earlier under-powered ceiling attempt was caught and corrected before trusting this |
| C27 | The round-robin-beats-EWMA falsifier reproduces on the real engine in the SAME direction at every stress level | `015f`: reproduces at below/near-ceiling, REVERSES at above-ceiling (EWMA's P99 3236ms beats round-robin's 4055ms) | Real engine, one topology | **LIMITED** — direction holds at 2/3 tested levels, documented exception at the third |
| C28 | The cache-affinity interim-latency effect (higher cache weight worsens interim latency, improves eventual recovery) is explained by committed backlog | `015d`: tested in both the originally-hypothesized location and the natural alternative; neither explains it; the metric itself becomes unstable at this scale | One B1-style scenario | **UNRESOLVED** — explicitly not claimed to be explained |
| C29 | Smoothing alpha is causally responsible for Adaptive's H2 sustained-lag behavior | `013g`: swap-window degraded-share decreases strictly and monotonically as alpha increases (70%→47%) | H2-style rapid target-quality-change scenario only | SUPPORTED, scenario-scoped |
| C30 | Smoothing alpha similarly explains the MAIN capacity-boundary collapse | `014g`/`015b` F3: no alpha value ever lets EWMA beat round-robin at the main boundary; committed backlog does not decrease monotonically with alpha | Main-boundary and canonical scenarios | **RETIRED as a general claim** — alpha's causal role is specific to H2, not the main collapse mechanism (though `015b` F3 found SOME structural effect, e.g. changing which target locks up) |
| C31 | This project's results are byte-for-byte reproducible given a seed | `internal/vtime`'s identity tests; every Stage 11-16 experiment reruns cleanly from its committed seed | Same machine/toolchain, virtual engine only — see `Stage16-ReproducibilityAudit` section of `Stage16.md` for engine-specific caveats (real engine, wall-clock timing, is NOT byte-identical by nature) | SUPPORTED, scope-limited — see reproducibility levels table |
| C32 | The flagship scenario's conclusion (Adaptive/EWMA show severe, often-non-draining acute collapse; RR/WRR/LC/P2C stay comparatively mild) holds across independent seeds, not just one | `016-flagship`: 3 independent seeds (16000-16002) with genuine arrival-stream jitter, consistent qualitative outcome in every seed | Canonical scenario | SUPPORTED |

This ledger is the authoritative cross-reference for every strong claim in `README.md` and the Stage
artifacts. If a future edit to this project makes a claim not traceable to a row here (or to a new row
added with the same rigor), that claim should be treated as unsupported until it is.
